version: 2
updates:
  # Maintain dependencies for Python via pip (Dependabot natively supports uv.lock)
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
    # Automatically apply security updates as soon as they are found
    target-branch: "main"
