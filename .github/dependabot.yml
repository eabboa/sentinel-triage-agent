version: 2
updates:
  # Maintain dependencies for Python via uv
  - package-ecosystem: "uv"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 50
    # Automatically apply security updates as soon as they are found
    target-branch: "main"
